name: my_flutter_app
description: A new Flutter project with a splash screen
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    get: ^4.6.5 # Add this line (use the latest version if available)

  # Add any additional dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # Add assets here if needed
  # assets:
  #   - assets/images/